/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    // domains: ['image.tmdb.org'],
    domains: ['img.freepik.com'],
  }
}

module.exports = nextConfig
